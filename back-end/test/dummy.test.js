/*eslint-disable*/
import { assert } from "chai";

describe("Sample Test Suite", () => {
  it("should pass this test", () => {
    assert.equal(1 + 1, 2);
  });

  it("should fail this test", () => {
    assert.equal(2 * 2, 5);
  });
});
/*eslint-enable*/